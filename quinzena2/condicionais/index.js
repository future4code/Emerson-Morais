// Exercícios de interpretação
   // 1. a) O código vai coletar um número, dividir por dois e 
   // e comparar o resto da divisão com zero. Se igual for passar vai passar no teste,
   // se for impar não vai passar.

   //    b) para números que o resto seja zero.

   //    c) para o resto que não é igual a zero.


   // 2. a) Para pesquisar uma fruta e informar o preço correspondente.

    //   b) O preço da fruta Maçã é R$2.25

    //   c) O preço da fruta Pêra é R$ 5
    

    // 3. a) Pedindo um número para o usuário.

    //    b) Se for 10 a mensagem será "Esse número passou no teste", pois zero é maior que dez.
    //       Em seguida dará erro pois dentro da função não tem o else para informar se o número
    //       inserido pelo usuário é menor que dez, então a mensagem seria secreta.

    //    c) Sim, haverá erro. 


    // Exercícios de escrita de código

    // 1.
    //  

    /* const idade = Number(prompt("Qual é a sua idade?"))

    function LicençaParaDirigir(maiorDeIdade) {
        if (maiorDeIdade > 18) {
            return "Você pode dirigir!"
        }   else {
            return "Você não pode dirigir!"
        }
    }
        console.log(LicençaParaDirigir(idade))  */


        // 2)
            /* const turno = prompt("Qual turno você estuda? responda com M V ou N:")
            
            function turnoDeEstudo(turnoQueEstuda) {
                if (turnoQueEstuda === "M")
                return "Bom dia!";
                if (turnoQueEstuda === "V")
                return "Boa tarde!";
                if (turnoQueEstuda === "N")
                return "Boa noite!"
           
            }  
        
            console.log(turnoDeEstudo(turno))  */
    


            // 3) 
            
            /* const turno = prompt("Qual turno você estuda? responda com M V ou N:")

            switch (turno) {
                case "manhã":
                    console.log("Você estuda no período da manhã")
                    break
                    case "vespertino":
                    console.log("Você estuda a tarde")
                    break
                    case "noite":
                    console.log("Você estuda a noite")
                    default:
                        
            }   */


            // 4)
            
            /* const qualfilme = prompt("Qual o gênero do filme que irão assistir?")
            const preçoIngresso = Number(prompt("Qual o valor do ingresso?"))

            function preçoEhIngresso(qualfilme, preçoIngresso) {
                if (qualfilme === "Fantasia" && preçoIngresso<15) {
                    return "Bom filme!"
                }   else {
                    return "Escolha outro filme!"

                }
                }
            
                console.log(preçoEhIngresso(qualfilme, preçoIngresso)) */


              